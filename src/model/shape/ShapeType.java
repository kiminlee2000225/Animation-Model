package model.shape;

/**
 * An enum to determine the shape type of the shape. The currently available shape types are
 * rectangles and ellipses. This enum helps distinguish the different types of shapes. This enum is
 * required because the model needs to distinguish different types of shapes to draw and animate
 * specific shapes.
 */
public enum ShapeType {
  RECTANGLE, ELLIPSE
}
